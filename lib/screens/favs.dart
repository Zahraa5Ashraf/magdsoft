import 'package:flutter/material.dart';

class favorites extends StatelessWidget {
  const favorites({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Container(
        color: Colors.blue, child: Center(child: Text("Favourites")));
  }
}
